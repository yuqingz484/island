# 保密与风控要点

> 以下要点提炼自本次自动化审阅实践中的安全反思。

### 3.1 权限最小化：用 API Key 而非账号密码

账号密码拥有后台全部操作权限（批改日记、管理学员、财务操作等），一旦泄露后果不可控。若需让外部工具或 LLM 代理执行自动化任务，应使用权限受限的 API Key，将其操作范围限定在特定资源集合内（如仅可读写某一期候选池），即使发生误操作，爆炸半径也可控。

**原则：** 允许的最大破坏 = 可承受的最大损失。候选池最坏情况不过是重新推荐，但若误删日记则无法恢复。

### 3.2 绝不将账号密码直接交给大模型

将账号密码输入给 LLM 存在以下风险：

- **训练数据残留**：模型供应商可能将对话数据用于训练，你的凭证随之进入训练集，后续可被他人通过 prompt 提取。
- **中转站风险**：使用 AI 中转/代理服务时，凭证在中间层被明文截获的概率大幅上升。
- **即使正规厂商也需警惕**：虽然头部厂商不稀罕你的凭证，但你无法控制数据在内部流转的每一环。

**结论：** 脑子里始终要有这根弦——一旦密码给了大模型，就视为已泄露。

### 3.3 Skills 供应链攻击

开源 Skills 的威胁模型：

1. 一个 Skills 初期完全安全、功能好用，积累大量用户
2. 作者在后续更新中植入恶意指令或木马代码
3. 用户不知情下自动更新，原本的工具变成后门

Skill 本质是可执行指令的集合，不只是静态文档。其中的任何一句话——比如"清空垃圾站"——模型都会视为可执行命令，且执行后果真实不可逆。

### 3.4 文档中的指令即代码

放在桌面上的 Markdown 文档并不天然安全。Skills 文档是 SOP + 可执行指令的复合体，模型读取后会将其中描述的操作步骤实际执行。这意味着：

- 文档中任何操作描述都可能被模型执行
- 破坏性指令一旦被写入文档，其破坏力与直接运行脚本无异
- 需要像审查代码一样审查 Skills 文档中的每一个操作指令

### 3.5 两条实操底线

日常使用中做到两点即可大幅降低风险：

1. **每个 Skills 下载后先不运行，让大模型先审查。** 将 Skills 内容粘贴给模型，要求它检查是否包含恶意指令、可疑的网络请求、文件删除/覆盖操作、或权限越界行为，确认安全后再使用。
2. **Skills 永不开启自动更新。** 关闭任何形式的自动更新功能。即使一个 Skills 初期安全，作者也可能在后续版本中植入恶意代码。手动更新前同样先走审查流程。
